# LABS
There are 4 `super_table_col_name` that appear in Emory's final supertables but not in MIMIC's grouping file because there are no corresponding variables that can be categorized into them.\
**['fio2', 'mtp', 'prealbumin', 'procalcitonin']**

There are 3 `super_table_col_name` that are marked as `import` in MIMIC's grouping file, but not in Emory's final supertables.\
**['bilirubin_indirect', 'cortisol', 'progesterone']**
